---
const today = new Date();
---

<footer>
	<div class="footer-content">
        
        <nav class="footer-links">
			<a href="mailto:hello@jaywyss.com">Email</a>
			<a href="https://bsky.app/profile/jaywyss.bsky.social" target="_blank">BlueSky</a>
			<a href="/resume">Résumé</a>
		</nav>

        <div class="credits">
			&copy; {today.getFullYear()} Jay Wyss. Powered by <a href="https://astro.build" target="_blank">Astro</a>. Co-developed with <a href="https://deepmind.google/technologies/gemini/" target="_blank">Gemini</a>.
		</div>

	</div>
</footer>

<style>
	footer {
		padding: 3em 1em 4em 1em;
		background: linear-gradient(var(--gray-gradient)) no-repeat;
		color: rgb(var(--gray));
		text-align: center;
		margin-top: 4rem;
		border-top: 1px solid rgba(0,0,0,0.05);
	}

	.footer-content {
		max-width: 720px;
		margin: 0 auto;
		display: flex;
		flex-direction: column;
		gap: 1.5rem; /* Consistent spacing between rows */
		align-items: center;
	}

	/* --- TOP ROW --- */
	.footer-links {
		display: flex;
		gap: 2rem;
        margin-bottom: 0.5rem; /* Slight extra breathing room */
	}

	.footer-links a {
		color: var(--text-main);
		text-decoration: none;
		font-weight: 700;
		font-size: 1rem;
		transition: color 0.2s;
	}

	.footer-links a:hover {
		color: var(--accent);
	}

	/* --- MIDDLE ROW (Credits) --- */
	.credits {
		font-size: 0.8rem;
		color: var(--text-muted);
        opacity: 0.7; 
	}
    
    .credits a {
        color: inherit;
        text-decoration: none;
        border-bottom: 1px dashed var(--text-muted);
    }
    
    .credits a:hover {
        color: var(--text-main);
        border-bottom-style: solid;
    }

    /* --- BOTTOM ROW (Meta) --- */
    .meta-link a {
        font-size: 0.75rem; /* Even smaller/subtler */
        color: var(--text-muted);
        text-decoration: none;
        opacity: 0.5;
        transition: opacity 0.2s;
    }

    .meta-link a:hover {
        opacity: 1;
        text-decoration: underline;
        color: var(--accent);
    }
</style>